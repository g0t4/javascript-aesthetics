describe("methodNames", function () {
    "use strict";
    /*eslint no-unused-vars: 0, no-undef: 0 */

    it.skip("how about this?", function () {

        var p = m(args);

        //var p = max(args);

        var maximumCost = maximum(prices);

        account.credit(10);
        account.withdrawl(5);

        account.getBalance();
        account.getPendingTransactions();

        account.balance();
        account.pendingTransactions();

        database.save(person);
        database.saveAndReturnId(person);

        function doLaundryAndWashCarAndFeedCat() { }

        function doLaundry() {  }

        function washCar() {  }

        function feedCat() {  }

        function getWeatherHistoryAndPredictWeeklyForecast(city) {  }

        function getWeeklyForecast(city) {  }

    });
});
